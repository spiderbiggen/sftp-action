{
    "name": "sftp-action",
    "version": "0.1.0",
    "private": true,
    "description": "A Github Action to help deploy projects using SFTP",
    "main": "dist/index.js",
    "scripts": {
        "build": "ncc build src/index.js -C -m"
    },
    "dependencies": {
        "@actions/core": "^1.5.0",
        "@actions/glob": "^0.2.0",
        "ssh2-sftp-client": "^7.0.3"
    },
    "devDependencies": {
        "@vercel/ncc": "^0.30.0"
    }
}
